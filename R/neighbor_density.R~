neighbor.density <- function(neigh.dists, D, k, N){

    return(exp(log(k)
               - ( log(N)
                  + log(unit.hypersphere.volume(D))
                  + D * log(neigh.dists)
                  )
               )
           );
}
